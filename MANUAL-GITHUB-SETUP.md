# GitHub手动设置指南

## 🔧 解决字符编码问题

如果批处理文件出现乱码，请按以下步骤手动操作：

## 📋 手动步骤

### 1. 在GitHub创建仓库
1. 访问 https://github.com
2. 点击右上角 "+" → "New repository"
3. 仓库名：`machine-code-tool`
4. 选择 Public 或 Private
5. 点击 "Create repository"

### 2. 生成个人访问令牌
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 选择权限：`repo` (完整仓库访问)
4. 点击 "Generate token"
5. **复制并保存令牌**

### 3. 手动执行Git命令

打开PowerShell或CMD，执行以下命令：

```bash
# 1. 清理Git配置
git config --global http.sslVerify false
git config --global credential.helper ""

# 2. 初始化仓库
git init
git add .
git commit -m "Initial commit: Machine Code Tool"

# 3. 添加远程仓库（替换用户名和令牌）
git remote add origin https://你的用户名:你的令牌@github.com/你的用户名/machine-code-tool.git

# 4. 推送到GitHub
git branch -M main
git push -u origin main
```

### 4. 验证自动构建

推送成功后：
1. 访问 `https://github.com/你的用户名/machine-code-tool/actions`
2. 查看构建状态
3. 等待构建完成（约10-20分钟）
4. 从 Artifacts 下载构建产物

## 🎯 简化脚本

如果字符编码正常，可以使用：

```bash
# 使用修复版本
github-setup-fixed.bat

# 或使用简化版本
upload-simple.bat
```

## 🔗 重要链接

- **创建仓库**: https://github.com/new
- **个人令牌**: https://github.com/settings/tokens
- **Actions文档**: https://docs.github.com/en/actions

## 💡 提示

1. **令牌安全**: 不要泄露个人访问令牌
2. **仓库权限**: 确保令牌有足够权限
3. **网络问题**: 可能需要科学上网
4. **构建时间**: GitHub Actions构建需要10-20分钟

## 🆘 常见错误

### 错误1: 推送被拒绝
**解决**: 检查令牌权限和仓库地址

### 错误2: 网络超时
**解决**: 使用VPN或代理

### 错误3: 字符编码问题
**解决**: 使用英文版脚本或手动执行命令

按照这个指南，你就能成功上传到GitHub并启动自动构建了！