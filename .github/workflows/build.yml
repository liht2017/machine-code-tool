name: 跨平台构建

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v2.1.0)'
        required: false
        default: 'latest'

jobs:
  build:
    strategy:
      fail-fast: false  # 不因为一个平台失败而取消其他平台
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: machine-code-tool.exe
            asset_name: machine-code-tool-windows
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: machine-code-tool
            asset_name: machine-code-tool-macos
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            artifact_name: machine-code-tool
            asset_name: machine-code-tool-kylin

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: 安装Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        targets: ${{ matrix.target }}
    
    - name: 添加备选目标
      if: matrix.target == 'x86_64-unknown-linux-musl'
      run: rustup target add x86_64-unknown-linux-gnu

    - name: 缓存Cargo
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          src-tauri/target
        key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-${{ matrix.target }}-
          ${{ runner.os }}-cargo-

    - name: 安装Linux依赖和静态链接工具
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        
        echo "🔧 安装静态链接构建所需的完整工具链..."
        
        # 安装基本构建工具
        sudo apt-get install -y build-essential curl wget pkg-config
        
        # 安装musl工具链（用于完全静态链接）
        sudo apt-get install -y musl-tools musl-dev gcc-multilib
        
        # 安装开发库（包括静态库）
        sudo apt-get install -y libwebkit2gtk-4.1-dev || sudo apt-get install -y libwebkit2gtk-4.0-dev
        sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev
        
        # 尝试安装静态版本的库
        sudo apt-get install -y \
          libgtk-3-dev \
          libglib2.0-dev \
          libcairo2-dev \
          libpango1.0-dev \
          libgdk-pixbuf2.0-dev \
          libatk1.0-dev \
          libssl-dev \
          2>/dev/null || echo "部分静态库安装失败，继续..."
        
        # 检查可用的静态库
        echo "📋 检查可用的静态库："
        find /usr/lib -name "*.a" | grep -E "(gtk|glib|webkit|ssl)" | head -20 || echo "未找到相关静态库"
        
        # 检查musl工具
        echo "🔍 检查musl工具："
        which musl-gcc || echo "musl-gcc未找到"
        musl-gcc --version 2>/dev/null || echo "musl-gcc版本检查失败"

    - name: 缓存Tauri CLI
      uses: actions/cache@v4
      with:
        path: ~/.cargo/bin/cargo-tauri
        key: ${{ runner.os }}-tauri-cli-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-tauri-cli-

    - name: 安装Tauri CLI
      run: |
        if ! command -v cargo-tauri &> /dev/null; then
          echo "Installing Tauri CLI..."
          cargo install tauri-cli --locked
        else
          echo "Tauri CLI already installed"
          cargo tauri --version
        fi
      shell: bash

    - name: 构建
      run: |
        cd src-tauri
        echo "开始Tauri构建..."
        echo "目标平台: ${{ matrix.target }}"
        echo "当前目录: $(pwd)"
        echo "Rust版本: $(rustc --version)"
        echo "Cargo版本: $(cargo --version)"
        echo "Tauri版本: $(cargo tauri --version)"
        
        # 简化构建：所有平台都只生成可执行文件，避免打包问题
        echo "构建可执行文件，跳过复杂打包"
        echo "使用Rust原生TLS，无需OpenSSL依赖"
        
        if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
          echo "🎯 Linux构建：专注于静态链接构建（无需用户安装依赖）"
          
          # 方法1: 尝试musl静态链接（最彻底的静态链接）
          echo "🔧 方法1: 尝试musl完全静态链接..."
          export PKG_CONFIG_ALL_STATIC=1
          export RUSTFLAGS="-C target-feature=+crt-static -C link-arg=-static"
          
          if cargo tauri build --target ${{ matrix.target }} --no-bundle --verbose; then
            echo "✅ musl静态链接构建成功（完全无依赖）"
            STATIC_BUILD=true
          else
            echo "❌ musl静态链接失败，尝试glibc静态链接..."
            
            # 清除之前的构建
            cargo clean
            
            # 方法2: 尝试glibc静态链接
            echo "🔧 方法2: 尝试glibc静态链接..."
            export RUSTFLAGS="-C target-feature=+crt-static"
            unset PKG_CONFIG_ALL_STATIC
            
            if cargo tauri build --target x86_64-unknown-linux-gnu --no-bundle --verbose; then
              echo "✅ glibc静态链接构建成功（部分静态）"
              STATIC_BUILD=partial
            else
              echo "❌ glibc静态链接也失败，尝试优化的musl构建..."
              
              # 清除之前的构建
              cargo clean
              
              # 方法3: 优化的musl静态构建
              echo "🔧 方法3: 优化musl环境和依赖..."
              
              # 安装更多musl相关工具
              sudo apt-get install -y musl-tools musl-dev gcc-multilib
              
              # 设置musl交叉编译环境
              export CC_x86_64_unknown_linux_musl=musl-gcc
              export CXX_x86_64_unknown_linux_musl=musl-g++
              export AR_x86_64_unknown_linux_musl=ar
              export CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER=musl-gcc
              
              # 尝试禁用某些可能有问题的特性
              export PKG_CONFIG_ALL_STATIC=1
              export RUSTFLAGS="-C target-feature=+crt-static"
              
              if cargo tauri build --target x86_64-unknown-linux-musl --no-bundle --verbose; then
                echo "✅ 优化musl静态构建成功（完全无依赖）"
                STATIC_BUILD=true
              else
                echo "❌ 所有静态链接尝试都失败了"
                echo "🔄 回退到动态链接 + 便携包方案..."
                
                # 最后的回退方案：动态链接 + 便携包
                unset RUSTFLAGS PKG_CONFIG_ALL_STATIC CC_x86_64_unknown_linux_musl CXX_x86_64_unknown_linux_musl AR_x86_64_unknown_linux_musl CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER
                
                if cargo tauri build --target x86_64-unknown-linux-gnu --no-bundle --verbose; then
                  echo "⚠️ 使用动态链接构建（将创建便携包包含依赖）"
                  STATIC_BUILD=false
                else
                  echo "❌ 连动态链接都失败了，构建彻底失败"
                  exit 1
                fi
              fi
            fi
          fi
          
          echo "后处理：根据构建类型处理文件"
          echo "当前目录: $(pwd)"
          echo "查找构建产物位置..."
          find . -name "machine-code-tool" -type f 2>/dev/null || true
          
          # 确定正确的构建目录（我们在src-tauri目录下）
          if [ -f "target/${{ matrix.target }}/release/machine-code-tool" ]; then
            BUILD_DIR="target/${{ matrix.target }}/release"
            ACTUAL_TARGET="${{ matrix.target }}"
          elif [ -f "target/x86_64-unknown-linux-gnu/release/machine-code-tool" ]; then
            BUILD_DIR="target/x86_64-unknown-linux-gnu/release"
            ACTUAL_TARGET="x86_64-unknown-linux-gnu"
          else
            echo "❌ 找不到构建产物，列出target目录内容："
            ls -la target/ 2>/dev/null || echo "target目录不存在"
            find target -name "machine-code-tool" -type f 2>/dev/null || echo "未找到machine-code-tool文件"
            exit 1
          fi
          
          echo "使用构建目录: $BUILD_DIR"
          echo "实际目标: $ACTUAL_TARGET"
          cd "$BUILD_DIR"
          
          # 检查是否为静态链接
          echo "检查构建类型..."
          if ldd machine-code-tool | grep -q "not a dynamic executable"; then
            echo "✅ 检测到静态链接可执行文件"
            STATIC_BUILD=true
          elif ldd machine-code-tool | grep -q "libwebkit2gtk"; then
            echo "⚠️ 检测到动态链接，需要创建便携包"
            STATIC_BUILD=false
          else
            echo "✅ 检测到部分静态链接"
            STATIC_BUILD=partial
          fi
          
          # 根据构建类型处理
          if [ "$STATIC_BUILD" = "true" ]; then
            echo "✅ 静态链接成功，无需便携包"
            # 直接复制静态可执行文件
            mkdir -p static-app
            cp machine-code-tool static-app/
            echo "静态链接版本已准备完成" > static-app/README.txt
            echo "此版本无需安装任何依赖，直接运行即可" >> static-app/README.txt
            tar -czf machine-code-tool-static.tar.gz static-app/
          else
            echo "⚠️ 需要创建便携包"
            # 创建便携目录结构
            mkdir -p portable-app/lib
            cp machine-code-tool portable-app/
          
          # 分析并复制依赖库（更全面的复制）
          echo "分析依赖库..."
          ldd machine-code-tool | grep "=> /" | awk '{print $3}' > /tmp/deps.txt
          
          # 复制所有非系统核心库
          while read lib; do
            if [ -f "$lib" ]; then
              # 排除系统核心库，复制应用相关库
              if [[ "$lib" != "/lib/"* && "$lib" != "/lib64/"* && "$lib" != *"libc.so"* && "$lib" != *"libdl.so"* && "$lib" != *"libpthread.so"* && "$lib" != *"libm.so"* && "$lib" != *"ld-linux"* ]]; then
                echo "复制库: $lib"
                cp "$lib" portable-app/lib/ 2>/dev/null || true
                
                # 同时复制这个库的依赖
                ldd "$lib" 2>/dev/null | grep "=> /" | awk '{print $3}' | while read sublib; do
                  if [ -f "$sublib" ] && [[ "$sublib" != "/lib/"* && "$sublib" != "/lib64/"* && "$sublib" != *"libc.so"* && "$sublib" != *"libdl.so"* && "$sublib" != *"libpthread.so"* && "$sublib" != *"libm.so"* && "$sublib" != *"ld-linux"* ]]; then
                    cp "$sublib" portable-app/lib/ 2>/dev/null || true
                  fi
                done
              fi
            fi
          done < /tmp/deps.txt
          
          # 创建启动脚本（增强版）
          echo '#!/bin/bash' > portable-app/machine-code-tool-launcher
          echo '# 机器码获取工具启动脚本' >> portable-app/machine-code-tool-launcher
          echo 'DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"' >> portable-app/machine-code-tool-launcher
          echo '' >> portable-app/machine-code-tool-launcher
          echo '# 设置库路径' >> portable-app/machine-code-tool-launcher
          echo 'export LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"' >> portable-app/machine-code-tool-launcher
          echo 'export PKG_CONFIG_PATH="$DIR/lib/pkgconfig:$PKG_CONFIG_PATH"' >> portable-app/machine-code-tool-launcher
          echo '' >> portable-app/machine-code-tool-launcher
          echo '# 设置GTK相关环境变量' >> portable-app/machine-code-tool-launcher
          echo 'export GDK_PIXBUF_MODULE_FILE="$DIR/lib/gdk-pixbuf-2.0/2.10.0/loaders.cache"' >> portable-app/machine-code-tool-launcher
          echo 'export GDK_PIXBUF_MODULEDIR="$DIR/lib/gdk-pixbuf-2.0/2.10.0/loaders"' >> portable-app/machine-code-tool-launcher
          echo '' >> portable-app/machine-code-tool-launcher
          echo '# 检查依赖' >> portable-app/machine-code-tool-launcher
          echo 'if [ ! -f "$DIR/machine-code-tool" ]; then' >> portable-app/machine-code-tool-launcher
          echo '    echo "错误: 找不到主程序文件"' >> portable-app/machine-code-tool-launcher
          echo '    exit 1' >> portable-app/machine-code-tool-launcher
          echo 'fi' >> portable-app/machine-code-tool-launcher
          echo '' >> portable-app/machine-code-tool-launcher
          echo '# 启动程序' >> portable-app/machine-code-tool-launcher
          echo 'echo "启动机器码获取工具..."' >> portable-app/machine-code-tool-launcher
          echo 'exec "$DIR/machine-code-tool" "$@"' >> portable-app/machine-code-tool-launcher
          chmod +x portable-app/machine-code-tool-launcher
          
          # 创建依赖检查脚本
          echo '#!/bin/bash' > portable-app/check-deps.sh
          echo '# 检查依赖库' >> portable-app/check-deps.sh
          echo 'DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"' >> portable-app/check-deps.sh
          echo 'echo "检查依赖库..."' >> portable-app/check-deps.sh
          echo 'export LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"' >> portable-app/check-deps.sh
          echo 'ldd "$DIR/machine-code-tool" | grep "not found" && echo "❌ 缺少依赖库" || echo "✅ 依赖库完整"' >> portable-app/check-deps.sh
          chmod +x portable-app/check-deps.sh
          
          # 显示便携包内容
          echo "便携包内容:"
          ls -la portable-app/
          echo "包含的库:"
          ls -la portable-app/lib/
          
            # 打包为tar.gz
            tar -czf machine-code-tool-portable.tar.gz portable-app/
            
            echo "✅ 创建了便携版本"
          fi
          
          # 显示最终结果
          echo "最终构建产物:"
          ls -la *.tar.gz 2>/dev/null || ls -la machine-code-tool
        else
          cargo tauri build --target ${{ matrix.target }} --no-bundle --verbose
        fi
        
        # 检查构建结果
        echo "构建完成，检查输出文件..."
        find target -name "machine-code-tool*" -type f || echo "未找到构建产物"
      shell: bash

    - name: 构建失败时的调试信息
      if: failure()
      run: |
        echo "构建失败，收集调试信息..."
        echo "=== 系统信息 ==="
        uname -a || echo "无法获取系统信息"
        echo "=== 环境变量 ==="
        env | grep -E "(RUST|CARGO|PATH)" || echo "无法获取环境变量"
        echo "=== 磁盘空间 ==="
        df -h || echo "无法获取磁盘信息"
        echo "=== 构建日志 ==="
        if [ -f "src-tauri/target/${{ matrix.target }}/release/build/*/out/build.log" ]; then
          cat src-tauri/target/${{ matrix.target }}/release/build/*/out/build.log
        fi
      shell: bash

    - name: 查找构建文件
      run: |
        echo "查找Linux构建产物..."
        if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
          echo "在src-tauri目录中查找..."
          find src-tauri/target -name "machine-code-tool*" -type f 2>/dev/null || echo "未在src-tauri/target找到文件"
          echo "检查可能的目录结构..."
          ls -la src-tauri/target/ 2>/dev/null || echo "src-tauri/target目录不存在"
          ls -la src-tauri/target/${{ matrix.target }}/ 2>/dev/null || echo "${{ matrix.target }}目录不存在"
          ls -la src-tauri/target/${{ matrix.target }}/release/ 2>/dev/null || echo "release目录不存在"
          ls -la src-tauri/target/x86_64-unknown-linux-gnu/release/ 2>/dev/null || echo "gnu release目录不存在"
        else
          echo "查找其他平台构建产物..."
          find src-tauri/target -name "machine-code-tool*" -type f || true
          ls -la src-tauri/target/${{ matrix.target }}/release/ || true
        fi
      shell: bash

    - name: 复制并重命名构建产物
      run: |
        mkdir -p upload
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          # Windows: 复制exe文件，保持.exe后缀
          cp src-tauri/target/${{ matrix.target }}/release/machine-code-tool.exe upload/${{ matrix.asset_name }}.exe
        elif [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
          # Linux: 查找并复制构建产物
          echo "查找Linux构建产物..."
          
          # 确定构建目录
          if [ -f "src-tauri/target/${{ matrix.target }}/release/machine-code-tool" ]; then
            BUILD_DIR="src-tauri/target/${{ matrix.target }}/release"
          elif [ -f "target/${{ matrix.target }}/release/machine-code-tool" ]; then
            BUILD_DIR="target/${{ matrix.target }}/release"
          else
            echo "❌ 找不到Linux构建产物"
            exit 1
          fi
          
          # 按优先级复制版本
          if [ -f "$BUILD_DIR/machine-code-tool-static.tar.gz" ]; then
            echo "复制静态链接版本..."
            cp "$BUILD_DIR/machine-code-tool-static.tar.gz" upload/${{ matrix.asset_name }}-static.tar.gz
            echo "✅ Linux静态版本已准备完成（无需依赖）"
          elif [ -f "$BUILD_DIR/machine-code-tool-portable.tar.gz" ]; then
            echo "复制便携版本..."
            cp "$BUILD_DIR/machine-code-tool-portable.tar.gz" upload/${{ matrix.asset_name }}-portable.tar.gz
            echo "✅ Linux便携版本已准备完成"
          else
            echo "使用标准版本..."
            cp "$BUILD_DIR/machine-code-tool" upload/${{ matrix.asset_name }}
            chmod +x upload/${{ matrix.asset_name }}
            echo "✅ Linux标准版本已准备完成"
          fi
        else
          # macOS: 复制文件并添加执行权限
          cp src-tauri/target/${{ matrix.target }}/release/machine-code-tool upload/${{ matrix.asset_name }}
          chmod +x upload/${{ matrix.asset_name }}
          echo "✅ macOS文件已准备完成"
        fi
        ls -la upload/
      shell: bash

    - name: 生成平台说明文件
      run: |
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          echo "🚀 机器码获取工具 - Windows版" > upload/使用说明.txt
          echo "=========================" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "✅ 使用方法:" >> upload/使用说明.txt
          echo "双击 machine-code-tool-windows.exe 运行即可" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "📋 功能说明:" >> upload/使用说明.txt
          echo "- 获取本机硬件信息（MAC地址、主板序列号等）" >> upload/使用说明.txt
          echo "- 生成唯一机器码用于软件授权" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "❓ 常见问题:" >> upload/使用说明.txt
          echo "- 如果杀毒软件拦截，请添加信任" >> upload/使用说明.txt
          echo "- 程序运行时请勿关闭黑色窗口" >> upload/使用说明.txt
        elif [ "${{ matrix.os }}" = "macos-latest" ]; then
          echo "🍎 机器码获取工具 - macOS版" > upload/使用说明.txt
          echo "========================" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "✅ 使用方法:" >> upload/使用说明.txt
          echo "双击 machine-code-tool-macos 文件运行即可" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "📋 功能说明:" >> upload/使用说明.txt
          echo "- 获取本机硬件信息（MAC地址、主板序列号等）" >> upload/使用说明.txt
          echo "- 生成唯一机器码用于软件授权" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "🔒 安全提示:" >> upload/使用说明.txt
          echo "如提示'无法验证开发者':" >> upload/使用说明.txt
          echo "- 右键点击文件 → 选择'打开'" >> upload/使用说明.txt
          echo "- 或到 系统偏好设置 → 安全性与隐私 → 点击'仍要打开'" >> upload/使用说明.txt
        else
          # 根据构建结果生成不同的说明
          if ls upload/*-static.tar.gz >/dev/null 2>&1; then
            echo "🐧 机器码获取工具 - 银河麒麟版（静态版本）" > upload/使用说明.txt
            echo "=======================================" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "🎉 此版本为静态链接版本，真正做到开箱即用！" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "✨ 版本特点:" >> upload/使用说明.txt
            echo "• 无需安装任何系统依赖库" >> upload/使用说明.txt
            echo "• 无需root权限安装软件包" >> upload/使用说明.txt
            echo "• 适用于任何Linux发行版（包括银河麒麟）" >> upload/使用说明.txt
            echo "• 下载解压即可使用" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "✅ 使用方法:" >> upload/使用说明.txt
            echo "1. 解压文件: 右键点击 .tar.gz 文件 → 解压" >> upload/使用说明.txt
            echo "2. 进入解压后的 static-app 文件夹" >> upload/使用说明.txt
            echo "3. 双击 machine-code-tool 启动程序" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "💡 如果双击无效:" >> upload/使用说明.txt
            echo "右键点击文件 → 属性 → 权限 → 勾选"允许作为程序执行" → 再次双击" >> upload/使用说明.txt
          elif ls upload/*-portable.tar.gz >/dev/null 2>&1; then
            echo "🐧 机器码获取工具 - 银河麒麟版（便携版本）" > upload/使用说明.txt
            echo "=======================================" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "✅ 使用方法:" >> upload/使用说明.txt
            echo "1. 解压文件: 右键点击 .tar.gz 文件 → 解压" >> upload/使用说明.txt
            echo "2. 进入解压后的 portable-app 文件夹" >> upload/使用说明.txt
            echo "3. 双击 machine-code-tool-launcher 启动程序" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "💡 如果双击无效:" >> upload/使用说明.txt
            echo "右键点击 machine-code-tool-launcher → 属性 → 权限 → 勾选执行权限 → 再次双击" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "🔧 如果仍然报错:" >> upload/使用说明.txt
            echo "1. 在 portable-app 文件夹中双击 check-deps.sh 检查依赖" >> upload/使用说明.txt
            echo "2. 根据检查结果安装缺失的库文件" >> upload/使用说明.txt
          else
            echo "🐧 机器码获取工具 - 银河麒麟版（标准版本）" > upload/使用说明.txt
            echo "=======================================" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "✅ 使用方法:" >> upload/使用说明.txt
            echo "1. 右键点击 machine-code-tool-kylin 文件" >> upload/使用说明.txt
            echo "2. 选择"属性" → "权限" → 勾选"允许作为程序执行文件"" >> upload/使用说明.txt
            echo "3. 双击文件运行" >> upload/使用说明.txt
            echo "" >> upload/使用说明.txt
            echo "❓ 如果无法启动:" >> upload/使用说明.txt
            echo "可能需要安装系统依赖: sudo yum install -y webkit2gtk4.1" >> upload/使用说明.txt
            echo "或者: sudo apt install -y libwebkit2gtk-4.1-0" >> upload/使用说明.txt
          fi
          echo "" >> upload/使用说明.txt
          echo "📋 功能说明:" >> upload/使用说明.txt
          echo "- 获取本机硬件信息（MAC地址、主板序列号等）" >> upload/使用说明.txt
          echo "- 生成唯一机器码用于软件授权" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
          echo "💡 提示:" >> upload/使用说明.txt
          echo "- 适用于银河麒麟桌面操作系统" >> upload/使用说明.txt
          echo "- 程序启动后会显示操作界面" >> upload/使用说明.txt
          echo "- 如需关闭程序，点击窗口关闭按钮即可" >> upload/使用说明.txt
          echo "" >> upload/使用说明.txt
        fi
      shell: bash

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.asset_name }}
        path: upload/

  collect:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: 下载所有构建产物
      uses: actions/download-artifact@v4
      with:
        pattern: machine-code-tool-*
        merge-multiple: true
      
    - name: 整理文件并添加正确扩展名
      run: |
        mkdir -p release
        
        echo "当前目录文件:"
        ls -la
        
        # 处理所有下载的文件
        for file in machine-code-tool-*; do
          if [ -f "$file" ]; then
            echo "处理文件: $file"
            cp "$file" "release/$file"
            chmod +x "release/$file"
          fi
        done
        
        # macOS文件已经在上面的循环中处理了
        
        
        ls -la release/
      shell: bash
        
    - name: 上传最终产物
      uses: actions/upload-artifact@v4
      with:
        name: all-platforms-${{ github.run_number }}
        path: release/
        
    - name: 创建Release (仅标签推送时)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
        name: 机器码获取工具 ${{ github.ref_name }}
        body: |
          ## 🚀 机器码获取工具 ${{ github.ref_name }}
          
          ### 📦 下载链接
          - **Windows**: machine-code-tool-windows.exe
          - **macOS Intel**: machine-code-tool-macos-intel  
          - **macOS ARM**: machine-code-tool-macos-arm
          - **银河麒麟**: machine-code-tool-kylin
          
          ### ✨ 功能特性
          - 跨平台硬件信息获取
          - HTTP API服务 (端口18888)
          - 图形用户界面
          - 授权状态管理
          
          ### 📋 API接口
          - `GET /api/machine-code` - 获取机器码信息
          - `GET /api/auth-status` - 检查授权状态  
          - `POST /api/set-auth` - 设置授权状态
          - `GET /api/health` - 健康检查
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}