name: 跨平台构建

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v2.1.0)'
        required: false
        default: 'latest'

jobs:
  build:
    strategy:
      fail-fast: false  # 不因为一个平台失败而取消其他平台
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: machine-code-tool.exe
            asset_name: machine-code-tool-windows.exe
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: machine-code-tool
            asset_name: machine-code-tool-macos
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: machine-code-tool
            asset_name: machine-code-tool-kylin

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: 安装Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        targets: ${{ matrix.target }}

    - name: 缓存Cargo
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          src-tauri/target
        key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-${{ matrix.target }}-
          ${{ runner.os }}-cargo-

    - name: 安装Linux依赖
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential curl wget libssl-dev pkg-config
        # Try webkit2gtk-4.1 first (Ubuntu 24.04+), fallback to 4.0 (older versions)
        sudo apt-get install -y libwebkit2gtk-4.1-dev || sudo apt-get install -y libwebkit2gtk-4.0-dev
        sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev
        # Additional dependencies that might be needed
        sudo apt-get install -y libjavascriptcoregtk-4.1-dev libsoup-3.0-dev || sudo apt-get install -y libjavascriptcoregtk-4.0-dev libsoup2.4-dev

    - name: 缓存Tauri CLI
      uses: actions/cache@v4
      with:
        path: ~/.cargo/bin/cargo-tauri
        key: ${{ runner.os }}-tauri-cli-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-tauri-cli-

    - name: 安装Tauri CLI
      run: |
        if ! command -v cargo-tauri &> /dev/null; then
          echo "Installing Tauri CLI..."
          cargo install tauri-cli --locked
        else
          echo "Tauri CLI already installed"
          cargo tauri --version
        fi
      shell: bash

    - name: 构建
      run: |
        cd src-tauri
        echo "开始Tauri构建..."
        echo "目标平台: ${{ matrix.target }}"
        echo "当前目录: $(pwd)"
        echo "Rust版本: $(rustc --version)"
        echo "Cargo版本: $(cargo --version)"
        echo "Tauri版本: $(cargo tauri --version)"
        
        # 对于Linux，只构建可执行文件，不打包
        if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
          echo "Linux构建：只生成可执行文件，跳过AppImage打包"
          cargo tauri build --target ${{ matrix.target }} --no-bundle --verbose
        else
          echo "其他平台构建：生成完整包"
          cargo tauri build --target ${{ matrix.target }} --verbose
        fi
        
        # 检查构建结果
        echo "构建完成，检查输出文件..."
        find target -name "machine-code-tool*" -type f || echo "未找到构建产物"
      shell: bash

    - name: 构建失败时的调试信息
      if: failure()
      run: |
        echo "构建失败，收集调试信息..."
        echo "=== 系统信息 ==="
        uname -a || echo "无法获取系统信息"
        echo "=== 环境变量 ==="
        env | grep -E "(RUST|CARGO|PATH)" || echo "无法获取环境变量"
        echo "=== 磁盘空间 ==="
        df -h || echo "无法获取磁盘信息"
        echo "=== 构建日志 ==="
        if [ -f "src-tauri/target/${{ matrix.target }}/release/build/*/out/build.log" ]; then
          cat src-tauri/target/${{ matrix.target }}/release/build/*/out/build.log
        fi
      shell: bash

    - name: 查找构建文件
      run: |
        echo "查找构建产物..."
        find src-tauri/target -name "machine-code-tool*" -type f || true
        ls -la src-tauri/target/${{ matrix.target }}/release/ || true
      shell: bash

    - name: 复制并重命名构建产物
      run: |
        mkdir -p upload
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          # Windows: 直接复制exe文件
          cp src-tauri/target/${{ matrix.target }}/release/machine-code-tool.exe upload/${{ matrix.asset_name }}
        else
          # macOS/Linux: 复制文件并添加执行权限
          cp src-tauri/target/${{ matrix.target }}/release/machine-code-tool upload/${{ matrix.asset_name }}
          chmod +x upload/${{ matrix.asset_name }}
          
          # 为macOS创建.app应用包，可直接双击运行
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            APP_NAME="${{ matrix.asset_name }}.app"
            mkdir -p "upload/$APP_NAME/Contents/MacOS"
            mkdir -p "upload/$APP_NAME/Contents/Resources"
            
            # 复制可执行文件到app包内
            cp "upload/${{ matrix.asset_name }}" "upload/$APP_NAME/Contents/MacOS/machine-code-tool"
            chmod +x "upload/$APP_NAME/Contents/MacOS/machine-code-tool"
            
            # 创建Info.plist (使用echo避免heredoc语法问题)
            echo '<?xml version="1.0" encoding="UTF-8"?>' > "upload/$APP_NAME/Contents/Info.plist"
            echo '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '<plist version="1.0">' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '<dict>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleExecutable</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>machine-code-tool</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleIdentifier</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>com.xinyu.machine-code-tool</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleName</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>机器码获取工具</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleDisplayName</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>机器码获取工具</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleVersion</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>2.1.0</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundleShortVersionString</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>2.1.0</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>CFBundlePackageType</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>APPL</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>LSMinimumSystemVersion</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <string>10.13</string>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>NSHighResolutionCapable</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <true/>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <key>LSUIElement</key>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '    <false/>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '</dict>' >> "upload/$APP_NAME/Contents/Info.plist"
            echo '</plist>' >> "upload/$APP_NAME/Contents/Info.plist"
            
            echo "✅ 已创建macOS应用包: $APP_NAME"
            ls -la "upload/$APP_NAME/Contents/MacOS/"
          fi
        fi
        ls -la upload/
      shell: bash

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.asset_name }}
        path: upload/

  collect:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: 下载所有构建产物
      uses: actions/download-artifact@v4
      with:
        pattern: machine-code-tool-*
        merge-multiple: true
      
    - name: 整理文件并添加正确扩展名
      run: |
        mkdir -p release
        
        echo "当前目录文件:"
        ls -la
        
        # 处理所有下载的文件
        for file in machine-code-tool-*; do
          if [ -f "$file" ]; then
            echo "处理文件: $file"
            cp "$file" "release/$file"
            chmod +x "release/$file"
          fi
        done
        
        # 创建使用说明
        cat > release/README.txt << 'EOF'
        🚀 机器码获取工具 - 使用说明
        ================================
        
        📦 文件说明:
        - machine-code-tool-windows.exe  → Windows系统 (双击运行)
        - machine-code-tool-macos-intel  → Intel Mac (终端: ./machine-code-tool-macos-intel)  
        - machine-code-tool-macos-arm    → Apple Silicon Mac (终端: ./machine-code-tool-macos-arm)
        - machine-code-tool-kylin        → 银河麒麟系统 (终端: ./machine-code-tool-kylin)
        
        🌐 访问界面:
        程序启动后，打开浏览器访问: http://localhost:3030
        
        📋 API接口:
        - GET  /api/machine-code  → 获取机器码
        - GET  /api/auth-status   → 检查授权状态
        - POST /api/set-auth      → 设置授权状态
        
        ⚠️  macOS安全提示:
        如果提示"无法验证开发者"，请到 系统偏好设置 → 安全性与隐私 → 点击"仍要打开"
        EOF
        
        ls -la release/
      shell: bash
        
    - name: 上传最终产物
      uses: actions/upload-artifact@v4
      with:
        name: all-platforms-${{ github.run_number }}
        path: release/
        
    - name: 创建Release (仅标签推送时)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
        name: 机器码获取工具 ${{ github.ref_name }}
        body: |
          ## 🚀 机器码获取工具 ${{ github.ref_name }}
          
          ### 📦 下载链接
          - **Windows**: machine-code-tool-windows.exe
          - **macOS Intel**: machine-code-tool-macos-intel  
          - **macOS ARM**: machine-code-tool-macos-arm
          - **银河麒麟**: machine-code-tool-kylin
          
          ### ✨ 功能特性
          - 跨平台硬件信息获取
          - HTTP API服务 (端口18888)
          - 图形用户界面
          - 授权状态管理
          
          ### 📋 API接口
          - `GET /api/machine-code` - 获取机器码信息
          - `GET /api/auth-status` - 检查授权状态  
          - `POST /api/set-auth` - 设置授权状态
          - `GET /api/health` - 健康检查
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}